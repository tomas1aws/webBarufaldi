"use client";
import { useEffect, useState } from "react";
import { Menu, X } from "lucide-react";
import { Container } from "@/components/ui/Container";
const nav=[['Estudio','#estudio'],['Áreas de práctica','#areas'],['Experiencia','#experiencia'],['Clientes','#clientes'],['Contacto','#contacto']];
export function Header(){const [open,setOpen]=useState(false);const [scrolled,setScrolled]=useState(false);useEffect(()=>{const update=()=>setScrolled(scrollY>20);update();addEventListener('scroll',update,{passive:true});return()=>removeEventListener('scroll',update)},[]);useEffect(()=>{document.body.style.overflow=open?'hidden':'';return()=>{document.body.style.overflow=''}},[open]);return <header className={`header ${scrolled||open?'header--solid':''}`}><Container className="header__inner"><a className="wordmark" href="#inicio" aria-label="Barufaldi Abogados, inicio"><strong>BARUFALDI</strong><span>ABOGADOS</span></a><nav className="nav nav--desktop" aria-label="Navegación principal">{nav.slice(0,4).map(([label,href])=><a key={href} href={href}>{label}</a>)}<a className="nav__cta" href="#contacto">Contactanos</a></nav><button className="menu-button" onClick={()=>setOpen(!open)} aria-expanded={open} aria-controls="mobile-navigation" aria-label={open?'Cerrar menú':'Abrir menú'}>{open?<X/>:<Menu/>}</button></Container><nav id="mobile-navigation" className={`nav-mobile ${open?'is-open':''}`} aria-label="Navegación móvil">{nav.map(([label,href],i)=><a key={href} href={href} onClick={()=>setOpen(false)}><span>0{i+1}</span>{label}</a>)}</nav></header>}
