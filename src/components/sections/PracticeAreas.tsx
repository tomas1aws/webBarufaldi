"use client";
import { useState } from "react";import { Plus } from "lucide-react";import { practiceAreas } from "@/data/practiceAreas";import { Container } from "@/components/ui/Container";import { SectionLabel } from "@/components/ui/SectionLabel";
export function PracticeAreas(){const [active,setActive]=useState(0);return <section className="section practices" id="areas"><Container><div className="section-heading"><SectionLabel>Áreas de práctica</SectionLabel><h2>Soluciones jurídicas para cada etapa del negocio.</h2></div><div className="practices__layout"><div className="practices__list">{practiceAreas.map((area,i)=><div className={`practice ${active===i?'is-active':''}`} key={area.number}><button onClick={()=>setActive(i)} aria-expanded={active===i} aria-controls={`area-${i}`}><span>{area.number}</span><strong>{area.title}</strong><Plus aria-hidden="true"/></button><div className="practice__mobile-content" id={`area-${i}`}><p>{area.description}</p><ul>{area.services.map(s=><li key={s}>{s}</li>)}</ul></div></div>)}</div><aside className="practice__panel" aria-live="polite"><span>{practiceAreas[active].number} / 04</span><h3>{practiceAreas[active].title}</h3><p>{practiceAreas[active].description}</p><ul>{practiceAreas[active].services.map(s=><li key={s}>{s}</li>)}</ul></aside></div></Container></section>}
