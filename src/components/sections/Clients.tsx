import { Container } from "@/components/ui/Container";import { SectionLabel } from "@/components/ui/SectionLabel";import { clients } from "@/data/clients";import { Reveal } from "@/components/ui/Reveal";
export function Clients(){return <section className="section clients" id="clientes"><Container><div className="clients__head"><SectionLabel>Confían en nosotros</SectionLabel><p>Empresas y referentes que han elegido nuestro acompañamiento jurídico.</p></div><Reveal className="clients__grid">{clients.map((client,i)=><div key={client}><span>{String(i+1).padStart(2,'0')}</span><strong>{client}</strong></div>)}</Reveal></Container></section>}
