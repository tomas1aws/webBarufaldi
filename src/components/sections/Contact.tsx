import { ArrowUpRight } from "lucide-react";import { Container } from "@/components/ui/Container";import { SectionLabel } from "@/components/ui/SectionLabel";import { contact } from "@/data/contact";
export function Contact(){const hasDetails=contact.email||contact.phone||contact.address;return <section className="section contact" id="contacto"><Container><SectionLabel light>Contacto</SectionLabel><div className="contact__grid"><h2>Conversemos sobre cómo podemos acompañar a tu empresa.</h2><div>{hasDetails?<address>{contact.email&&<a href={`mailto:${contact.email}`}>{contact.email}</a>}{contact.phone&&<a href={`tel:${contact.phone}`}>{contact.phone}</a>}{contact.address&&<span>{contact.address}</span>}</address>:<p className="contact__note">Canal de contacto en proceso de actualización.<br/>Buenos Aires · Argentina</p>}contact.email&&<a className="contact__link" href={`mailto:${contact.email}`}>Contactanos <ArrowUpRight aria-hidden="true"/></a></div></div></Container></section>}
